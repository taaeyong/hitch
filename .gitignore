# Python / general
__pycache__/
*.pyc
.venv/
.env

# Private raw relationship data: track only safe scaffold docs/placeholders.
raw/**
!raw/
!raw/README.md
!raw/**/
!raw/**/.gitkeep

# Private source drops that may appear at repo root.
*.csv
*.txt

# Generated derived wiki/simulation data: keep tracked for demo history.
# Raw private inputs remain ignored above; derived outputs under data/ are intentionally tracked.

# Local runtime / operator files
ops/run-status.md
